import { createElement } from 'react'

/** Minimal icon seam: the plugin test suite does not need the host's full visual library. */
export function IconChevronDownOutline14({ className }: { readonly className?: string }) {
  return createElement('svg', { className, 'aria-hidden': true })
}
